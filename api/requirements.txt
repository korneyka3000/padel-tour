# What Vercel installs for the serverless function.
#
# It lives next to the function on purpose. At the repository root Vercel ignored it and
# installed the project from pyproject.toml instead, which has no base dependencies — so the
# function deployed with padel_tour present and every one of its imports missing.
#
# The package itself still arrives via that `pip install .`, so it is not listed here.
# uvicorn is absent too: Vercel provides the server.
#
# tests/test_requirements.py keeps this list honest against pyproject.toml.
fastapi>=0.141
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30
aiosqlite>=0.20
aiogram>=3.15
python-dotenv>=1.0
